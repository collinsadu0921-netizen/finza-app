// Credit notes are available on all tiers (starter+).
// This re-export keeps the URL under /service/* so tier context
// and sidebar active-state detection work correctly.
export { default } from "@/app/credit-notes/page"
