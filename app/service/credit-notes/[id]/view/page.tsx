// Credit note detail is available on all service tiers (starter+).
// Keep URL under /service/* for tier context and active sidebar state.
export { default } from "@/app/credit-notes/[id]/view/page"
