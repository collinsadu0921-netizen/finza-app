{
  "name": "finza-hubtel-status-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Static-IP proxy for Hubtel transaction status checks (Finza → UpCloud → Hubtel)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0"
  }
}
