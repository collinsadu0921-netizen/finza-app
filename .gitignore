node_modules/
.next/
.env
.env.local
.env.*
!.env.staging.example
.vercel/
out/
dist/
build/
coverage/
*.log

# Playwright — saved Supabase session (never commit)
e2e/.auth/

# k6 load tests — real session cookies (never commit)
load-tests/sessions.json
load-tests/sessions.*.json
!load-tests/sessions.example.json
load-tests/results/
# Browser cURL captures for k6 session import (never commit)
*.curl
service-metrics.curl
.vercel
